import React from "react";
import styles from "../style";
import TopSlide from "./TopSlide";

const Hero = () => (
  <section className={`flex ${styles.paddingY}`}>
  </section>
);

export default Hero;
